# 08 Linux压缩打包

## 08.Linux压缩打包

）08.Linux压缩打包 。1.文件打包与压缩概述

### 11.1什么是文件压缩

### 1.2为什么需要文件压缩

### 1.3不同系统的压缩格式互通

### 11.4Linux下常见的压缩包类型

。2.文件打包与压缩-gzip ·2.1 gzip压缩文件

### 2.2 gzip应用场景

。3.文件打包与压缩-zip ■3.1 zip压缩文件 ·3.2unzip解压文件 ）4.文件打包与压缩-tar

### 4.1使用tar压缩文件

### 14.2使用tar列出文件

### 14.3使用tar解压文件

徐亮伟，多年互联网运维工作经验，曾负 责过大规模集群架构自动化运维管理工作。擅长 Web集群架构与自动化运维，曾负责国内大型电商运维工作。 ·本章课程内容大纲 。1.什么是文件压缩。 。2.为什公要对文件进行压缩。 o3.Windows和Linux压缩如何互通。 o4.Linux下常见压缩包类型 。5.gzip、zip、tar 工具实战

## 1.文件打包与压缩概述

### 1.1什么是文件压缩

·将多个文件或目录合并成为一个特殊的文件。

●比如：搬家...脑补画面img

### 1.2为什么需要文件压缩

，当我们在传输大量的文件时，通常都会选择将该文件进行压缩，然后在进行传输。 。首先：压缩后的文件会比压缩前的文件小。一个28G的文件夹压缩后能达到6G 。其次：多个文件传输很慢，但单个文件传输会很快，同时还能节省网络的消耗。 (比如：搬家时，单行李往外拿和打包后往外拿？你懂我意思？)

### 1.3不同系统的压缩格式互通

Windows的压缩包与Linux的压缩包能否互通？ 。在windows系统下，我们接触最多的压缩格式是rar或zip 。在Linux系统上使用最多的压缩格式是zip和tar·gz 。在Linux上的压缩格式放在windows系统下都是可以正常打开的。 ）所以一般Windows和Linux互通通常选择zip格式； ）值得注意的是Linux不支持Windows下的RAR格式的压缩文件；

### 1.4Linux下常见的压缩包类型

压缩工具 zip压缩工具 gzip压缩工具，只能压缩文件，会删除原文件(通常配合tar使用) bzip2压缩工具， 只能压缩文件，会删除原文件(通常配合tar使用) 先使用tar命令归档打包，然后使用gzip压缩 先使用tar命令归档打包，然后使用bzip压缩

## 2.文件打包与压缩-gzip

格式 .zip .gz .bz2 .tar.gz .tar.bz2 gzip仅能打包文件，并且打包后会删除源文件

### 2.1 gzip压缩文件

#对文件进行压缩

#查看gz压缩后的文件 #解压gzip的压缩包

### 2.2 gzip 应用场景

·使用场景：当需要让某个配置文件不生效时，且又不想删除； # -->Centos-Vault.repo.gz

## 3.文件打包与压缩-zip

·使用zip命令可以对文件进行压缩打包，解压则需要使用umzi

### 3.1 zip压缩文件

## 1.默认最小化安装的操作系统，没有zip和unzip

所以需要安装

## 2.使用zip压缩文件

ame.zip filename

## 3.使用zip压缩目录

]# zip-rdir.zip dir/

### 3.2 unzip解压文件

## 1.解压zip文件包，默认解压至当前目录

## 2.解压zip包内容至/opt目录

## 3.不解压压缩包，查看压缩包中的内容

## 4.文件打包与压缩-tar

tar命令是linux下最常用的压缩与解压缩，支持文件和目录的压缩归档； tar语法：tar[-zjxcvfpP]filename 含义 选项 使用gzip压缩归档后的文件(.tar.gz) 创建新的归档文件 Z 使用bzip2压缩归档后的文件(.tar.bz2 对归档文件解包 i 使用xz压缩归档后的文件(tar.xz) 列出归档文件里的文件列表 输出命令的归档或解包的过程 指定解压目录位置 指定包文件名，多参数f写最 排除多个文件 后 --exclud 排除文件或目录 e ·常用打包与压缩组合命令 组合含义 组合命令 组合含义 解压tar.gz格式文件 打包文件为tar.gz格式 JXZ 选项 C X 组合命令 czf jxf cjf 打包文件为tar.bz格式 解压tar.bz格式文件 打包文件为tar.xz格式 智能解压文件 cJf xf 查看压缩包文件内容 tf

### 4.1使用tar压缩文件

）1.将文件或目录进行打包压缩 #1。以gzip归档方式打包并压缩 #2.打包/tmp下所有文件 ·2.排除文件，并打包压缩 #1。排除单个文件 #2.排除多个文件 #3。将需要排除的文件写入文件中 etc/services etc/rc.local etc/rc.d/rc.local #指定需要排除的文件列表，最后进行打包压 paichu.Listetc/

### 4.2使用tar列出文件

·查看压缩包内容，但不解压 [root@xuliang #tar tftest.tar.gz

### 4.3使用tar解压文件

exclude=etc/rc.Loc aletc/ ·1.默认解压文件至当前目录 #1.解压至当前目录 ·2.指定解压内容存储至/opt目录

[root@student ~]# tar xf /etc/local.tar.gz -C /tmp Www xul i angwei. com
