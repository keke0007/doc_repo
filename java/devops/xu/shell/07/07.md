# 07.Shell数组应⽤

# 07.Shell数组应⽤

1.数组分类

2.普通数组

3.关联数组

4.遍历数组

徐亮伟, 江湖⼈称标杆徐。多年互联⽹运维⼯作经验，曾负责过⼤规模集群架构⾃动化运维管理⼯作。擅⻓Web集群架构与⾃动化运维，曾负责国内某⼤型电商运维⼯作。

个⼈博客"徐亮伟架构师之路"累计受益数万⼈。

笔者Q:552408925、572891887

架构师群:471443208

# 1.数组分类

普通数组：只能使⽤整数 作为数组索引

关联数组：可以使⽤字符串 作为数组索引

<table><tr><td colspan="4">name=bgx 变量</td></tr><tr><td>b</td><td>g</td><td colspan="2">x</td></tr><tr><td>0</td><td>1</td><td colspan="2">2</td></tr></table>

<table><tr><td colspan="3">books=(nginx mysql shell)普通数组</td></tr><tr><td>nginx</td><td>mysql</td><td>shell</td></tr><tr><td>0</td><td>1</td><td>2</td></tr></table>

<table><tr><td colspan="5">info=([name]=bgx [age]=18 [skill]=linux) 关联数组</td></tr><tr><td>bgx</td><td rowspan="2"></td><td>18</td><td rowspan="2"></td><td>linux</td></tr><tr><td>name</td><td>age</td><td>skill</td></tr></table>

# 2.普通数组

# 1.数组赋值⽅式


//针对每个索引进⾏赋值


```txt
[root@Shell ~]# array1[0]=pear
[root@Shell ~]# array1[1]=apple
[root@Shell ~]# array1[2]=orange
[root@Shell ~]# array1[3]=peach

//一次赋多个值,数组名=(多个变量值)
[root@Shell ~]# array2=(tom jack alice)
[root@Shell ~]# array3=(tom jack alice "bash shell")
[root@Shell ~]# array4=(1 2 3 "linux shell" [20]=puppet)
```


//将该⽂件中的每⼀个⾏作为⼀个元数赋 值给数组	array3


```txt
[root@Shell ~]# array5=(`cat /etc/passwd`) 
```

# 2.查看数组赋值结果

```txt
[root@Shell ~]# declare -a 
```

# 3.访问数组元数


//统计数组元数的个数


```txt
[root@Shell ~]# echo ${#array1[@]}
4 
```


//访问数组中的第⼀个元素


```txt
[root@Shell ~]# echo ${array1[0]}
pear 
```


//从数组索引1开始


```txt
[root@Shell ~]# echo ${array1[@]:1}
apple orange peach 
```


//从数组索引1开始 访问两个元素


```txt
[root@Shell ~]# echo ${array1[@]:1:2}
apple orange 
```


//访问数组中所有数据,相当于echo	${array1[*]}


```txt
[root@Shell ~]# echo ${array1[@]}
pear apple orange peach 
```

# 4.获取数组索引

```txt
//获取数组元数的索引
[root@Shell ~]# echo ${!array1[@]}
0 1 2 3
```

# 3.关联数组


1.定义关联数组, 申明是关联数据


```ini
[root@Shell ~]# declare -A tt_array_1
[root@Shell ~]# declare -A tt_array_2 
```


2.给关联数组进⾏赋值


```txt
数组名[索引]=变量值
[root@Shell ~]# tt_array1[index1]=pear
[root@Shell ~]# tt_array1[index2]=apple
[root@Shell ~]# tt_array1[index3]=orange
[root@Shell ~]# tt_array1[index4]=peach

//给关联数组一次赋多个值
[root@Shell ~]# tt_array2=([index1]=tom [index2]=jack [index3]=alice [index4]='bash shell')
```


3.查看关联数组


```txt
[root@Shell ~]# declare -A 
```


4.访问数据元数


```txt
//访问数组中的第二个元数
[root@Shell ~]# echo ${tt_array2[index2]}
jack
//访问数组中所有元数 等同于 echo ${array1[*]}
[root@Shell ~]# echo ${tt_array2[@]}
bash shell tom jack alice
//访问数组中所有元数的索引
```

```txt
[root@Shell ~]# echo ${!tt_array2[@]}
index4 index1 index2 index3 
```

# 4.遍历数组

1.通过数组元数的个数进⾏遍历(不推荐)

2.通过数组元数的索引进⾏遍历(推荐)

注意: 将统计的对象作为数组的索引, 仅针对关联数据


1.数据赋值与遍历


```shell
#!/usr/bin/bash
while read line
do
    hosts[++i]=$line
done </etc/hosts

echo "hosts first: ${hosts[1]""
echo

//for循环遍历方式
for i in ${!hosts[@]}
do
    echo "$i: ${hosts[i]}"
done

//for
#!/usr/bin/bash
IFS=$_'\n'

for line in `cat /etc/hosts`
do
    hosts[++j]=$line
done

for i in ${!hosts[@]}
do
    echo "$i: ${hosts[i]}"
done
```


2.统计 /etc/passwd 的 shell 数量


```shell
[root@Shell ~]# cat array_passwd_count.sh
#!/usr/bin/bash

declare -A array_passwd
#1. 对数组进行赋值
while read line
do
    type=$(echo $line|awk -F ':' '{print $NF}')
    let array_passwd[$type]++
done </etc/passwd
```


#2.对数组进⾏遍历


```shell
for i in ${!array_passwd[@]}
do
    echo 索引是:$i,索引的值是: ${array_passwd[$i]}
done
```


3.统计 Nginx ⽇志 IP 访问次数


```shell
[root@Shell ~]# cat array_nginx_count.sh
#!/usr/bin/bash
# nginx log top 10 IP conut
declare -A array_nginx
#1. 给关联数组的索引进行赋值
while read line
do
    type=$(echo $line|awk '{print $1}')
    let array_nginx[$type]++
done </var/log/nginx/access.log

for i in ${!array_nginx[@]}
do
    echo "IP是:$i 出现多少次${array_nginx[$i]}"
done
```


4.统计 tcp 的状态信息


```shell
[root@Shell ~]# cat array_ss_state.sh
#!/usr/bin/bash

declare -A array_state
type=$(ss -an |grep :80|awk '{print $2}') 
```


#1.对数组进⾏的索引赋值


```txt
for i in $type
do 
```

```shell
let array_state[$i]++
done
#2.遍历数组
for j in ${!array_state[@]}
do
    echo "当前的状态是:$j,当前状态出现了多少次:${array_state[$j]}"
done
```