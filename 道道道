简单分享我们团队在今年下半年技术方向规划：
1. [Agent Runtime]         
   - 业务支持与迭代
   - Framework & Multi-Agent
   - Tools / Skills / MCP（Action Layer）
   - Guardrails + HITL（策略侧）
2. [Cap & Performance]      
    - 稳定性 / 高可用
   - TTFT / E2E / 高并发
   - 语义缓存 + Smart Routing + 模型降级（需要能改执行路径）
3. [Context & Memory]   
    - 上下文管理、压缩、状态机
   - 长短期记忆
   - Knowledge & Retrieval（ingestion / hybrid search / rerank / citation）
4. [Platform & Infra]
    - AgentOps  
    - Sandbox隔离
    - Session 状态持久化
    - Managed API + Model Gateway（从 Runtime 移过来）
    - Harness 基础执行环境
5. [Data & Eval Loop]     
    - Trace & Observability
    - Eval Benchmark/Rubrics
    - 数据飞轮 + 自迭代



不知道你有没有这种体验：用 Codex 或者 Claude Code 干活，开头半小时它状态很好，改代码又快又准。但同一个会话用上两三个小时，它就开始犯糊涂——之前说好的约定它忘了，明明改过的文件它又改回去，你提醒一句它好一句，过几轮又犯。

多数人遇到这种时候，会觉得是模型不行，或者自己的提示词没写好。其实大概率是另一个问题：上下文腐化。

原理很简单。模型没有记忆，它每一轮回答都要把整段对话历史从头读一遍。会话越长，里面堆的东西越多：你两小时前让它读过的文件、调试失败时打印的几千行日志、中间推翻过的方案、早就无关的闲聊。每一轮它都要在这一大堆东西里找重点。内容越多，真正重要的信息被淹没得越厉害，它的注意力被摊得越薄。

而且有个规律：它对一段内容的开头和结尾记得最牢，塞在中间的信息最容易被忽略。你三小时前定下的技术方案，现在正好躺在它记忆最差的位置。

所以会话后期它变笨，不是模型退步了，是你给它的工作环境被垃圾填满了。

这对我们日常使用AI带来哪些实践上的指导?

第一条：一个会话只干一件事，干完就开新的。这是性价比最高的习惯。大任务拆成小任务，每个任务一个干净会话。新会话的上下文是空的，模型读到的全是相关信息，状态立刻回到最佳。很多人舍不得开新会话，觉得前面的上下文丢了可惜，其实那些上下文里八成是已经没用的调试记录和废弃方案，留着只会拖后腿。

第二条，学会交接，而不是延续。开新会话之前，让模型把当前进度写成一份简短的交接文档：目标是什么、改到哪了、改过哪些文件、下一步做什么。然后把这份文档贴给新会话。几十轮对话里真正有价值的信息，压缩完通常就几百字。Claude Code 的 compact 命令做的就是这个事情，但你手动让它写一份，质量往往更好，因为你可以要求它保留你关心的部分。

第三条，喂东西要精准。让它改代码，直接告诉它文件路径，比让它自己去整个仓库里翻要好得多。它自己翻，会把一堆不相关的文件读进上下文，每一份都在稀释注意力。报错日志也一样，贴关键的那几十行就够了，不要把几千行完整日志整段丢进去。你给它的每一段内容都会留在上下文里、之后每一轮都被重读，投喂之前值得过一遍脑子。

第四条，探索类的脏活让子代理去干。Claude Code 的 subagent、Codex 的并行任务都是这个用途。让子代理去搜代码库、读文档、跑测试，它那边上下文弄脏了无所谓，最后只把结论带回主会话。主会话的上下文保持干净，主代理的状态就一直在线。

第五条，配置文件别写成百科全书。CLAUDE.md、AGENTS.md 这类文件每一轮都在上下文里占位置，写得越长，留给干活的注意力越少。只写模型自己猜不到的规则：项目结构、命名约定、不能碰的目录。常识性的内容删掉，写了也是白占地方。

第六条，重要的指令放在消息的开头或结尾，不要埋在中间段落里。这是利用它的记忆特性，成本为零，收益直接。

第七条，长任务里主动监控。发现它开始重复犯同一个错、忘记前面的决定、改错文件，不要继续在同一会话里较劲，立即交接、开新会话。继续耗下去，每一轮都在为膨胀的上下文付双倍的费用，拿三成的效果。

上下文是模型唯一的工作台，也是唯一的记忆。

工作台乱，活就干不好。

用 AI 工具的水平差距，很大程度上不在提示词技巧，而在于学会去管理模型的上下文，就像我们保持用脑健康一样，也一样需要保持模型上下文的健康。



Agent Skills设计有一个很重要的原则：渐进式披露（progressive disclosure），下面给大家阐述一下它的重要性。

你在机场找登机口，一路上其实看不到整座机场的地图。你只会看到一块牌子：B区登机口，直行两百米。走完这两百米，下一块牌子才会出现。

如果机场把整座建筑的平面图、所有登机口的实时状态、每条跑道的运行情况全部贴在入口处，你反而很难找到路。

这就是渐进式披露，progressive disclosure。

它是交互设计里一个很老的原则：信息按需要分批出现，当前步骤只需要当前步骤的信息，剩下的藏起来，但不丢。

软件把高级设置收进折叠菜单，游戏的新手教程一关只教一个操作，说明书分成快速上手和完整手册两层，背后都是同一个想法。

这个想法最近被搬进了AI Agent的设计里，就是很多人开始用的skill。

一个skill本质上是一份写给模型看的操作手册，它的加载方式严格遵循渐进式披露。

第一层只有几十个字，说明这个skill叫什么、什么时候该用，这部分永远在模型的上下文里，成本极低。

第二层是手册正文，只有当模型判断当前任务真的需要它时才会读进来。

第三层是正文里引用的附件，比如脚本、模板、参考资料，只在确实用得上的那一刻才被加载。

为什么这样设计？因为模型的上下文是稀缺资源。

塞进去的东西越多，每一条能分到的注意力就越少，这是被反复验证过的事实。把所有信息一次性全给，看似周到，实际是把噪音和筛选的责任一起转嫁给了模型。

渐进式披露让信息成本和使用频率严格挂钩：永远在场的是索引，随取随用的是正文，压箱底的资料只在真正需要时才付成本。

仔细想想，人也是这样工作的。没人能把所有知识都背下来，高手脑子里装的其实是索引，知道遇到什么问题该去查什么资料、问什么人。

skill等于把这种工作方式在AI身上形式化了。

这个设计还有一个容易被忽略的好处：它让知识可以无限扩容。以前大家总觉得给模型的东西必须精简再精简，生怕塞爆上下文。

分层之后这个矛盾消失了，一个skill可以附带几十页的细节文档和完整的示例库，反正不用就不读。

写手册的人不用再纠结取舍，索引负责精简，附件负责详尽，两者可以共存。

这个原则在任何信息过载的场景里都成立。

好的导师带新人，第一周只讲第一周用得上的东西，剩下的遇到再讲。

好的产品把大部分功能藏起来，只在用户产生对应意图的时候才露面。

渐进式披露说到底就是一种克制：在对的时间，给对的信息。

我们的口号是：保持用脑健康！保持用脑卫生！信息要渐进式披露！





简单分享我们团队在今年下半年技术方向规划：
1. [Agent Runtime]         
   - 业务支持与迭代
   - Framework & Multi-Agent
   - Tools / Skills / MCP（Action Layer）
   - Guardrails + HITL（策略侧）
2. [Cap & Performance]      
    - 稳定性 / 高可用
   - TTFT / E2E / 高并发
   - 语义缓存 + Smart Routing + 模型降级（需要能改执行路径）
3. [Context & Memory]   
    - 上下文管理、压缩、状态机
   - 长短期记忆
   - Knowledge & Retrieval（ingestion / hybrid search / rerank / citation）
4. [Platform & Infra]
    - AgentOps  
    - Sandbox隔离
    - Session 状态持久化
    - Managed API + Model Gateway（从 Runtime 移过来）
    - Harness 基础执行环境
5. [Data & Eval Loop]     
    - Trace & Observability
    - Eval Benchmark/Rubrics
    - 数据飞轮 + 自迭代

graph engineer
https://x.com/AdrianPunk115/status/2081268706483814605


