```javascript
ES7~ES13新特性(三)
```

## 目录

ES7新增特性解析 content ES8新增特性解析 ES10新增特性解析 ES11新增特性解析 ES12新增特性解析 ES13新增特性解析

## ES7 - Array Includes (已学)

1在ES7之前，如果我们想判断一个数组中是否包含某个元素，需要通过indexOf获取结果，并且判断是否为-1。 1在ES7中，我们可以通过includes来判断一个数组中是否包含一个指定的元素，根据情况，如果包含则返回true，否则返回false。

```javascript
arr.includes(valueToFind[,fromlndex])
if°(names.includes("why")){
console.log("包含why")
if°(names.includes("why", 4)) {
console.log("包含why")
console.log(names.indexof(NaN))/.-1
console.log(names.includes(NaN)).//.true
```

## ES7一指数exponentiation运算符(已学)

1在ES7之前，计算数字的乘方需要通过Math.pow方法来完成。 1在ES7中，增加了**运算符，可以对数字来计算乘方。

```javascript
const result1 = Math.pow(3, 3)
const result2 = 3 ** 3
console.log(resultl, result2)
```

## ES8 Object values

■之前我们可以通过Object.keys获取一个对象所有的key 在Es8中提供了Object.values来获取所有的value值：

```javascript
const obj=
```

name:"why" age:18, height:1.88

```javascript
console.log(object.Values(obj))//·[.why',18,1.88·]
```

- ·如果传入一个字符串
```javascript
console.log(object.values("abc"))
```

## ES8 Object entries

通过Object.entries可以获取到一个数组，数组中会存放可枚举属性的键值对数组。

- 可以针对对象、数组、字符串进行操作;
```javascript
const obj = {
```

name: "why", age: 18, height:·1.88

```javascript
console.log(object.entries(obj))·//·[·[·'name',·'why'·],·[·'age',·18·],·[.'height',·1.88·]·]
for (const entry of object.entries(obj)) {
const·[key, value] = entry
console.log(key,·value)
```

- /·如果是一个数组
```javascript
console.log(object.entries(["abc",·"cba",."nba"]))·//·[·[.'o',·'abc'·],·[·'1',·'cba'·],·[·'2',·'nba'·]·]
```

如果是一个字符串

```javascript
console.log(object.entries("abc"))·//.[·[.'0',
```

## ES8 - String Padding

1某些字符串我们需要对其进行前后的填充，来实现某种格式化效果，ES8中增加了padStart和padEnd方法，分别是对字符串 的首尾进行填充的。

```javascript
const message = "Hello World"
console.log(message.padStart(15, "a")) // aaaaHello World
console.log(message.padEnd(15, "b")) // Hello·Worldbbbb
```

1我们简单具一个应用场景：比如需要对身份证、银行卡的前面位数进行隐藏：

```javascript
const lastFourNumber = cardNumber.slice(-4)
const finalCardNumber = lastFourNumber.padStart(cardNumber.length, "*")
console.log(finalCardNumber)
```

## ES8 - Trailing Commas

在ES8中，我们允许在函数定义和调用时多加一个逗号：

```javascript
function foo(a,b,) {
console.log(a,°b)
f00(10，°20,)
```

## ES8 - Object Descriptors

```javascript
1 Object.getOwnPropertyDescriptors :
```

- 这个在之前已经讲过了，这里不再重复。
```javascript
1 Async Function: async、 await
```

- 后续讲完Promise讲解

## ES9新增知识点

Asynciterators：后续选代器讲解 1 Object spread operators：前面讲过了 Promisefinally：后续讲Promise讲解

## ES10 - flat flatMap

flat0方法会按照一个可指定的深度递归遍历数组，并将所有元素与遍历到的子数组中的元素合并为一个新数组返回。

```javascript
constnums=[10,20,5,8],[[2,3],[9,22]],100]
const newNumsl = nums.flat(1)
const newNums2= nums.flat(2)
```

1/·[·10,20,·5,8,[·2,3·],[·9,·22·],·100·]

```javascript
console.log(newNums1)
```

- /·[·10,·20,·5,·8,·2,·3,·9,·22,·100·]
```javascript
console.log(newNums2)
flatMap()方法首先使用映射函数映射每个元素，然后将结果压缩成一个新数组。
```

- 注意一：flatMap是先进行map操作，再做flat的操作;
- 注意二：flatMap中的flat相当于深度为1;
```javascript
const messages=·["Hello·World"，·"你好啊 李银河"，·"my·name is·why"]
const newMessages = messages.flatMap(item =>{
return item.split(".")
console.log(newMessages)
```

## ES10 - Object fromEntries

在前面，我们可以通过Object.entries将一个对象转换成entries 那么如果我们有一个entries了，如何将其转换成对象呢？

- ES10提供了Object.formEntries来完成转换：
那么这个方法有什么应用场景呢？

```javascript
const obj·= {
```

name: "why",

```javascript
age: 18, const searchParams = new URLSearchParams(paramsString)
height:·1.88 for (const param of searchParams)·{
console.log(param)
const entries = object.entries(obj) const searchobj = Object.fromEntries(searchParams)
console.log(entries) console.log(searchobj)
const info = Object.fromEntries(entries)
console.log(info)
```

## ES10 - trimStart trimEnd

1去除一个字符串首尾的空格，我们可以通过trim方法，如果单独去除前面或者后面呢？

- ES10中给我们提供了trimStart和trimEnd；
```javascript
const message =: I Hello World
console.log(message.trim())
console.log(message.trimStart())
console.log(message.trimEnd())
```

## ES10其他知识点

Symbol description：已经讲过了 1Optional catch binding：后面讲解try cach讲解

## ES11 - Biglnt

1在早期的JavaScript中，我们不能正确的表示过大的数字：

- 大于MAX_SAFE_INTEGER的数值，表示的可能是不正确的。
```javascript
const·maxInt = Number.MAX_SAFE_INTEGER
console.log(maxInt)
```

- /·大于MAX_SAFE_INTEGER值的一些数值，无法正确的表示
```javascript
console.log(maxInt + 1)·//·9007199254740992
console.log(maxInt +2)·//·9007199254740992
```

那么ES11中，引I入了新的数据类型Biglnt，用于表示大的整数：

- Bitlnt的表示方法是在数值的后面加上n
```javascript
const·bigInt =:9007199254740991n
console.log(bigInt + 1n)
console.log(bigInt + 2n)
```

## ES11 - Nullish Coalescing Operator

1ES11，NullishCoalescingOperator增加了空值合并操作符：

```javascript
const foo =
const°resultl = foo "默认值"
const result2·=·foo°??。"默认值"
console.log(resultl))//·默认值
console.log(result2)
```

## ES11 - Optional Chaining

可选链也是ES11中新增一个特性，主要作用是让我们的代码在进行null和undefined判断时更加清晰和简洁：

```javascript
const obj = {
friend: {
girlFriend: {
```

name: "lucy"

```javascript
if (obj.friend && obj.friend.girlFriend) {
console.log(obj.friend.girlFriend.name)
```

可选链的方式

```javascript
console.log(obj.friend?.girlFriend?.name)
```

## ES11 - Global This (已学)

1在之前我们希望获取JavaScript环境的全局对象，不同的环境获取的方式是不一样的

- 比如在浏览器中可以通过this、window来获取；
- 比如在Node中我们需要通过global来获取；
在ES11中对获取全局对象进行了统一的规范：globalThis

```javascript
console.log(globalThis)
console.log(this)°//·浏览器上
console.log(global) / / Node中
```

## ES11-for..in标准化

在ES11之前，虽然很多浏览器支持for..in来遍历对象类型，但是并没有被ECMA标准化。 在ES11中，对其进行了标准化，for..in是用于遍历对象的key的：

```javascript
const obj = {
```

name: "why", age: 18, height: 1.88

```javascript
for(const key inobj){
console.log(key)
```

## ES11其他知识点

DynamicImport：后续ESModule模块化中讲解。 1Promise.allSettled：后续讲Promise的时候讲解。 Iimportmeta：后续ESModule模块化中讲解。

## ES12 - FinalizationRegistry

FinalizationRegistry对象可以让你在对象被垃圾回收时请求一个回调。

- FinalizationRegistry提供了这样的一种方法：当一个在注册表中注册的对象被回收时，请求在某个时间点上调用一个清理回
```javascript
调。(清理回调有时被称为finalizer);
```

- 你可以通过调用register方法，注册任何你想要清理回调的对象，传入该对象和所含的值；
```javascript
let obj·= { name: "why" }
const registry = new FinalizationRegistry(value => {
console.log("对象被销毁了"，°value)
registry.register(obj,."obj")
obj·= null
```

## ES12-WeakRefs

如果我们默认将一个对象赋值给另外一个引用，那么这个引用是一个强引用：

- 如果我们希望是一个弱引l用的话，可以使用WeakRef；
```javascript
let obj·= { name:."why" }
let info = new WeakRef(obj)
```

## ES12 - logical assignment operators

- /·1.逻辑或运算符
```javascript
Let message
```

message message ll."hello·world"

```javascript
message I="Hello World"
console.log(message)
let obj=
```

name:"why" 1/·2.逻辑与操作符

- /·obj=·obj·&&·obj.foo()
```javascript
obj &&=obj.name
console.log(obj)
```

- /3.逻辑空运算符
```javascript
let foo =null
foo？？="默认值"
console.log(foo)
```

## ES12其他知识点

```javascript
Numeric Separator: 讲过了;
```

1String.replaceAll：字符串替换；

## ES13 - method .at0

前面我们有学过字符串、数组的at方法，它们是作为ES13中的新特性加入的：

- /·1.数组
```javascript
[.equ.. .eqo.. .oqe.] = saweu Je
console.log(names.at(1))
console.log(names.at(-1) )
```

- /~2.字符串
```javascript
Var str = "Hello-Coderwhy"
console.log(str.at(1))
console.log(str.at(-1))
```

## ES13 - Object.hasOwn(obj, propKey)

```javascript
1 Object中新增了一个静态方法(类方法)：hasOwn(obj,propKey)
```

- 该方法用于判断一个对象中是否有某个自己的属性；
那么和之前学习的Object.prototype.hasOwnProperty有什么区别呢?

```javascript
Note: object.hasOwn() is intended as a replacement for Object.hasOwnProperty()..
```

- 区别一：防止对象内部有重写hasOwnProperty
- 区别二：对于隐式原型指向null的对象，hasOwnProperty无法进行判断
```javascript
var obj = {
name: "why", var info =-Object.create(null)
age: 18, info.name = "why"
hasOwnProperty: function() { console.log(info.hasOwnProperty("name")) / / 报错
return false console.log(Object.hasOwn(info,"name")) / / 可以判断
```

## ES13 - New members of classes

```javascript
1在ES13中，新增了定义class类中成员字段(field)的其他方式：
Instance public fields class Person {
address = "中国"
Static public fields static totalCount = "7o亿"
```

Instance private fields

- /·只能类内部访问
```javascript
#sex = "male"
```

static private fields

```javascript
static #maleCount =:"10亿"
```

static block

```javascript
constructor(name, age)
this.name = name
this.age = age
static {
console.log("static-block execution")
printInfo() {
console.log(this.address, this.#sex, Person.#maleCount)
```
