# 项目打包和自动化部署

# 一.项目部署和DevOps

1.1.传统的开发模式 在传统的开发模式中，开发的整个过程是按部就班就行： OLD WAY: CODE BUILD TEST RELEASE OPERATE DEVELOPERS HANDOFF HANDOFF HANDOFF 但是这种模式存在很大的端：

- 工作的不协调：开发人员在开发阶段，测试和运维人员其实是处于等待的状态。等到测试阶段，开
发人员等待测试反馈bug，也会处于等待状态。

- 线上bug的隐患：项目准备交付时，突然出现了bug，所有人员需要加班、等待问题的处理；
1.2.DevOps开发模式 DevOps是Development和Operations两个词的结合，将开发和运维结合起来的模式： 开发 DEVOPS 运维

## E

## GEL

```javascript
DANiEL STORI (TUENOFF.US)
```

## NEW WAY:

BUILD CODE TEST 10101 OPERATE RELEASE

### 1.3.持续集成和持续交付

```javascript
伴随着DevOps一起出现的两个词就是持续集成和持续交付(部署)：
```

- Ci是ContinuousIntegration(持续集成)；
- CD是两种翻译：ContinuousDelivery(持续交付)或ContinuousDeployment(持续部署)；
持续集成CI: SOURCE BUILD REPOSITORY CISERVER TEST CHECK-IN RESULT 10101 10101 RESULT 持续交付和持续部署：

SOURCE BUILD REPOSITORY CISERVER ↑ TEST CHECK-IN RESULT 10101 10101 RESULT RESULT TEST RESULT STAGING RESULT MANUAL PRODUCTION SOURCE BUILD REPOSITORY CISERVER TEST CHECK-IN RESULT 0101 10101 RESULT RESULT TEST RESULT STAGING RESULT AUTO PRODUCTION

### 1.4.自动化部署流程

测试浏览器访问 Nginx配置 发布项目文件夹 测试服务器 安装Nginx Jenkins: TravisCI CircleCI Jenkins服务器 GithubActions 任务二 安装Java环境 安装Node/npm环境 构建测试项目 本地开发 push 比如GitHub Git仓库 安装Jenkins Jenkins自动构建 构建生产项目 任务一 SSH连接 用户浏览器访问 Nginx配置 发布项目文件夹 线上服务器 安装Nginx

## 二.购买云服务器

## 2.1.注册阿里云的账号

云服务器我们可以有很多的选择：阿里云、腾讯云、华为云。 目前在公司使用比较多的是阿里云； 我自己之前也一直使用阿里云，也在使用腾讯云；

- 之前华为云也有找我帮忙推广他们的活动；
但是在我们的课程中，我选择目前使用更加广泛的阿里云来讲解： 我们需要注册阿里云账号 https://aliyun.com/ 注册即可，非常简单

## 2.2.购买云服务器

购买云服务器其实是购买一个实例。

### 1.来到控制台：

阿里云 产品导服务 源索 云服务器ECS 我的资源 安全状态 云数据库RDS版专有网络VPC 云服务器 通行中 快照安全评分93分，有3条安全风险 Ouo立肆处理→

```javascript
×CDN A负载均衡 运行中(2级4GIB) 远程连接重息 常用功能
地址：华南3(广州)公网P: 8.134.60.235 0.28% CPU使用车内存使用率安装隔件云盘使用车安装督件 续费管理昆 权能配膜
```

城名 帮助与文档产品价格产品详情 云市场 我的教程 重要提醒 快速搭建网站 部署开发环境 搭建云上博客 本款程适用企业、丰富功能的网站。 通过简单几步搜款之苦 后台，还可扩展支撑小型门户网站。快速插健基于Wc ss的云上等客,W强大的管理 0免费下益！《玩精ECS从入门精通)进阶手册一本通，云上运维与架构必 LAMP 环境 读！立即快章3 搭建小程序 云上服务器迁移 云上高可用架构 错信小程序√ 提供ANS、Azure，腾讯云等主流云迁移数程，使用工具，可有效限您完成复杂迁移工作。 本教程活用关注稳定性的企业，智您构建一个安全，高可用和容灾能力强的系统。 Azure Linux系姚迁移至ECS v 设有我想要的场景订闻上新通知

## 2.创建实例，选择类型和配置

image-20201204152133800 mage-20201203103047329

## 3.配置网络安全组

```javascript
网络和安全组 ③系统配置(选埔) 分组设置(选填) 确认订单
```

网络 专有网络 如何选择网络 默认专有网络 默认交换机

```javascript
如胃创建新的专有网路，您可前在控制台创建> 交换机所在可用区：随桃分配交换机网段：
您可前往控制台创建>
```

公网IP公网带宽计费 分配公网IPv4 地址系挑会分配公网IP，也可果用更加灵活的弹性公网IP 方案， 带宽计费模式 按使用流量 按国定用宽

```javascript
后付费模式，按使用流量(单位为GB)计费，每小时扣费。请保证余额充足。
```

带宽峰值 25M 同里云免费提供最高5Gbps的恶意试量攻击防护，了解更多|提升防护能力 安全组 变全组类似防火墙功能，用于设置网络访 制台新建安全图安全FAG

```javascript
安全组股制配置安全地 所遗安全组1).默认安全组(自定义辖口)
请确保所选安全组开胶包含22(Linux)或者3389(Winde 3ows)端口，否则无活选程量录ECS，您可以进入ECS控制台设置。前往设置>
```

请匀选更开通的IP4的协议/题口：① HTTP 80 铺口 HTTPS 443口 22 8 3389 铺 ICMP 协①

```javascript
弹性网卡 通过弹性网卡，您可以实理离可用集群插建、低成本故薄转移和精细化的网络管理，了解更多>选择专有网络交换机方可配置弹性网卡
```

公网带宽：5MBps按使用流量 配置费用：￥0.513时公网流量费用：@￥0.800/GB 上一步：基础配置下一步：系坑配置确认订单 4.创

云服务器ECS 一键购买自定义购买 国产晶价格 产晶控制抢 确认订单 所选配置 基础配置 付费模式：签量付费 地域及可用区 大例

```javascript
锁量：CentOS7.64位(安全加图) 系统盘：ESSD云盘40GIB，随买例释度,PL0(单盘IOPS性能上用1万)
```

网络和安全组 VPC：默认专有网络 交换机：默认交换机

```javascript
公网带宽：授使用流量5M 变全组：1).默认安全组 (自定义属口)
```

系统配置 壹录凭证：自定义密码 使用时限 设置自动相放服务时间

```javascript
服务协议 (云服务器ECS服务条款)
```

订单对应的发票信息，请在管理控制台-费用中心-发票管理中查看。购买领知 丘产品默认禁用 TCP 25 增口和基于此增口的部箱服务，特殊情况氧按备审板后使用,查看评情： 公网带宽：5Mbps按使用流量 ￥0.513时公网流量费用：￥0.800/GB 上一步：分相设置创健实例

## 三.搭建服务器环境

## 3.1.jenkins自动化部署

### 3.1.1.安装Java环境

Jenkins本身是依赖lava的，所以我们需要先安装Java环境：

- 这里我安装了Java1.8的环境
dnf search java-1.8 dnf instal1 java-1.8.0-openjdk.x86_64

### 3.1.2.安装Jenkins

因为Jenkins本身是没有在dnf的软件仓库包中的，所以我们需要连接lenkins仓库：

- wget是Linux中下载文件的一个工具，-O表示输出到某个文件夹并且命名为什么文件；
)rpm：全称为TheRPMPackageManage，是Linux下一个软件包管理器； wget -0 /etc/yum.repos.d/jenkins.repo http://pkg.jenkins-ci.org/redhat- stable/jenkins.repo #导入GPG密钥以确保您的软件合法 rpm --import https://pkg.jenkins.io/redhat/jenkins.io.key #或者 rpm --import http://pkg.jenkins-ci.org/redhat/jenkins-ci.org.key 编辑一下文件/etc/yum.repos.d/jenkins.repo

- 可以通过vim编辑

[jenkins]

```javascript
name=Jenkins-stable
baseurl=http://pkg.jenkins.io/redhat
gpgcheck=1
```

安装Jenkins

```javascript
dnf install jenkins #--nogpgcheck(可以不加)
```

启动enkins的服务： systemctl start jenkins systemctl status jenkins Systemctl enable jenkins Jenkins默认使用8080端口提供服务，所以需要加入到安全组中：

```javascript
授权策略 协议类型 瑞口范围 授权类型(全部) 授权对象 描述 优先级 创建时间
```

- 允许 自定义TCP 8001/8001 IPv4地址段访问 0.0.0.0/0 2020年12月4日16:57
允许 自定义TCP 8080/8080 IPv4地址段访间 0.0.0.0/0 允许 自定义TCP IPv4地址段访间 0.0.0.0/0 允许 自定义 TCP 3306/3306 IPy4地址段访问 0.0.0.0/0 2020年12月3日 11:36 允许 自定义TCP 443/443 IPv4地址段访间 0.0.0.0/0 System

- 允许 自定义TCP 80/80 IPv4地址段访问 0.0.0.0/0 Systen
允许 自定义TCP 22/22 IPv4地址段访问 0.0.0.0/0 2020年12月3日10:43

```javascript
允许 全部 ICMP(IPv4) -1/-1 Pv4地址段访间 0.0.0.0/0 2020年12月3日10:43
```

3.1.3.Jenkins用户 我们后面会访问centos中的某些文件夹，默认Jenkins使用的用户是jenkins，可能会没有访问权限， 所以我们需要修改一下它的用户： 修改文件的路径：/etc/sysconfig/jenkins Welcome jenkins X

```javascript
etc>sysconfig> jenkins
```

## Type: string ## Default: "jenkins" ## ServiceRestart: jenkins # # Unix user account that runs the Jenkins daemon # Be careful when you change this, as you need to update # permissions of $JENKINs_HoME and /var/log/jenkins. # JENKINS_USER"root"

之后需要重启一下Jenkins: #也可以将Jenkins添加到root组中 sudousermod-a -G root jenkins Systemctlrestartjenkins 3.1.4.Jenkins配置 打开浏览器，输入：http://8.134.60.235:8080/

- 注意：你输入自己的IP地址
获取输入管理员密码：

- 在下面的地址中cat/var/lib/jenkins/secrets/initialAdminPassword
image-20201203173047824 可以安装推荐的插件：

## CustomizeJenkins

PluginsextendJenkinswithadditionalfeaturesto support many differentneeds. Install suggested Select plugins to plugins install Install plugins the Jenkins Selectand installplugins community findsmost mostsuitableforyourneeds. useful. 3.1.5.Jenkins任务 新建任务：

Dashboard- 新建任务 所有 用户列表 S W 名称 上次成功 构建历史 coderhub 20秒-#51 项目关系 图标：小中大 二检查文件指纹 系统管理 我的视图 Lockable Resources 新建视图 构建队列 队列中没有构建任务

## 输入一个任务名称

该字段不能为空，请输入一个合法的名称 构建一个自由风格的软件项目 这是Jenkins的主要功能Jenkins将会结合任何SCM和任何构建系统来构建你的项目，甚至可以构建软件以外的系统 流水线 的任务类型。 构建一个多配置项目 适用于多配置项目，例如多环境测试，平台指定构建，等等。 文件夹 创建一个可以嵌套存储的容器。利用它可以进行分组。视图仅仅是一个过滤器，而文件夹则是一个独立的命名空间，因此你可以有多个 相同名称的的内容，只要它们在不同的文件夹里即可。 GitHub组织

```javascript
扫描一个GitHub组织(或者个人账户)的所有仓库来匹配已定义的标记。
```

多分支流水线 配置项目和保留策略： General源码管理构建触发器构建环境构建构建后操作 描述 xue3_mal.cms 纯文本预览 GitHub 项目 This build requires lc Throtle bullds 丢弃旧的构建 参数化构建过程 ? 关闭构建 ? 在必要的时候并发构建 ? 源码管理：

General源码管理构建触发器构建环境构建构建后操作 源码管理 O无 GiIt ? Repositories ? Repository URL https://githul Credentials oderwh 添加 高级 Branches to build

```javascript
指定分支(为空时代表any)
```

- main
增加分支 构建触发器： 这里的触发器规则是这样的：

- 定时字符串从左往右分别是：分时日月周
#每半小时构建一次OR每半小时检查一次远程代码分支，有更新则构建 H/30**** #每两小时构建一次OR每两小时检查一次远程代码分支，有更新则构建

## HH/2＊**

#每天凌晨两点定时构建 H2*** #每月15号执行构建 HH 15** #工作日，上午9点整执行 H9**1-5 #每周1,3,5，从8:30开始，截止19:30，每4小时30分构建一次 H/30 8-20/4 ** 1,3,5 构建触发器

```javascript
触发远程构建(例如，使用脚本)
```

其他工程构建后触发 定时构建 日程表 HH...

```javascript
上次运行的时间 Wednesday, August 25, 2021 2:30:29 AM CST; 下次运行的时间 Thursday, August 26, 2021 2:30:29 AM CST.
GittHub hook triger for GITScm polling
```

轮询 SCM 构建环境： 注意：我们需要搭建Node的环境

- 第一步：配置Node的环境；
- 第二步：安装Node的插件;

Jenkins Dashboard 新建任务 管理Jenkins 用户列表 系统配置 第一步：配置Node环境的位置 第二步：安装Node插件的位置 构建历史 系统配置 全局工具配置 插件管理 项目关系 配置全局设置和路径 工具配置，包括它们的位置和自动安装器 添加、删除、装用或启用J xins功能扩展弧件

```javascript
检壹文件指纹 节点管理添加、删除、控制和监视系统运行任务的节点。 Managed files e.g. settings.xml for r
```

系统管理 custom files, 我的视图 安全 新建视图 全局安全配置Jenkins安全，定义准可以纺间或使用系统。 Manage Credentials 凭据配置配置凭据的提供者和类型 第一步：配置Node的环境 构建环境 Delete workspace before bulld starts

```javascript
Use secret text(s or fl(s)
```

Provide Contiguration files Abort the bulldif it's stuck Add timestamps to the Console Output

```javascript
Inspect bulld log for published Gradle buld scans
```

Provide Node & npm bin/ folder to PATH NodeJS Installation node14 npmrc file - use system default Cache location Default (~/.npm or %APP_DATA%\ with Ant 第二步：安装Node的插件 这里因为我已经安装过了，所以没有搜索到； Q nodeis 可更新 可选插件 已安装高级 安装 名称 版本 Released 使用上面的搜索框来检索可用的插件。 直接安装 下载待重启后安装1天1小时之前获取了更新信息立即获取 构建执行的任务：

- 查看Node的版本等是否有问题；
- 执行npminstall安装项目的依赖；
)移除原来mall_cms文件的所有内容；

- 将打包的dist文件夹内容移动到mall_cms文件夹；
pwd node-v npm -v npm install npm run build pwd echo'构建成功'

#删除/root/mal1_cms文件夹里所有的内容 rm -rf /root/mall_cms/* cp -rf./dist/*/root/mall_cms/ 构建 执行 shell 命令 npm npm install pwd echo"构建成功" BT 删除/root/mal1_cms文件夹里所有的内容

```javascript
cp =rf ./dist/→/zoot/mal1_cms/
```

宣看可用的环境变量列表 高级

## 3.2.nginx安装和配置

### 3.2.1.安装nginx

后续我们部署会使用nginx，所以需要先安装一下nginx: dnf install nginx 启动nginx: systemctl start nginx systemctl status nginx systemctl enable nginx

### 3.2.2.配置nginx

我们这里主要配置nginx的用户和默认访问目录：

- etc/nginx/nginx.conf
配置用户：

user root

```javascript
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;
```

通过Linux命令创建文件夹和文件： mkdir /root/mall_cms cd /root/ma11_cms touch index.html vi index.html 配置访问目录： server

```javascript
listen 80 default_server;
listen [::]:80 default_server;
```

server_name

```javascript
#·root /usr/share/nginx/html;
```

#·Load·configuration files·for·the default·server block.

```javascript
include /etc/nginx/default.d/*.conf;
```

location/

```javascript
root /root/mall_cms;
index index.html;
```
